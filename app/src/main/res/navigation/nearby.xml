<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nearby"
    app:startDestination="@id/nearby_page">
    <fragment
        android:id="@+id/nearby_page"
        android:name="foodie.app.rubikkube.foodie.fragments.NearByFragment"
        android:label="@string/title_nearby"
        tools:layout="@layout/fragment_nearby">
        <action
            android:id="@+id/action_title_to_about"
            app:destination="@id/nearby_page"/>
    </fragment>
</navigation>